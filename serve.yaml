# This file was generated using the `serve build` command on Ray v2.7.0.

proxy_location: EveryNode

http_options:

  host: 0.0.0.0

  port: 8000

grpc_options:

  port: 9000

  grpc_servicer_functions: []

applications:

- name: sklearn_model

  route_prefix: /

  import_path: src.serve:sklearn_model

  runtime_env: {}

  deployments:

  - name: SklearnModel
    ray_actor_options:
      runtime_env:
        pip:
        - joblib==1.3.2
        - scikit-learn==1.4.1.post1
      num_cpus: 1.0
